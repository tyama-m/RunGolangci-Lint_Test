# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1

orbs:
  win: circleci/windows@2.4.0

# Define a job to be invoked later in a workflow.
# See: https://circleci.com/docs/2.0/configuration-reference/#jobs
jobs:
  go-build:
    # Specify the execution environment. You can specify an image from Dockerhub or use one of our Convenience Images from CircleCI's Developer Hub.
    # See: https://circleci.com/docs/2.0/configuration-reference/#docker-machine-macos-windows-executor
    #docker:
      # CircleCI Go images available at: https://hub.docker.com/r/circleci/golang/
      #- image: circleci/golang:1.16
#    docker:
#      - image: cimg/base:stable
    # Add steps to the job
    # See: https://circleci.com/docs/2.0/configuration-reference/#steps
    executor:
      name: win/default
      size: medium

    steps:
      #- run: go install github.com/golangci/golangci-lint/cmd/golangci-lint@v1.44.0
      #- run: golangci-lint --version

      #- run: sqlite3 --version

      # - checkout

      # - run:
      #      name: "Go build test"
      #      command: go build main.go

      # - run:
      #     name: "Docker run image."
      #     shell: powershell.exe
      #     command: |
      #      docker run tyama1224/mdm-develop-image:0.2

       - run:
          name: "Golangci-lint install."
          shell: powershell.exe
          # command: curl -sSfL https://raw.githubusercontent.com/golangci/golangci-lint/master/install.sh | sh -s -- -b $(go env GOPATH)/bin v1.44.2
          command: go install github.com/golangci/golangci-lint/cmd/golangci-lint@v1.44.0

       - run:
          name: "golangci-lint version."
          command: golangci-lint --version

# Invoke jobs via workflows
# See: https://circleci.com/docs/2.0/configuration-reference/#workflows
workflows:
  go-build-workflow:
    jobs:
      - go-build
